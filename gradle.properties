org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# GitHub-Repository, aus dem die App Updates lädt (Format: benutzername/repo).
# Beim Bauen auf GitHub wird das automatisch gesetzt; hier nur für lokale Builds nötig.
updateRepo=
